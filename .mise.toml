[tools]
node = "20"

[tasks.install]
description = "Install dependencies"
run = "npm install"

[tasks.build]
description = "Build TypeScript project"
run = "npm run build"

[tasks.test]
description = "Run project tests"
run = "npm test"
